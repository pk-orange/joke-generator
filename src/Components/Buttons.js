import Jokes from "./JokeDisplay";


function Buttons({newJoke, jokeDisplayed}) {
    const handleNextOrPrev = (event) => {
        event.preventDefault();
        const whichButton =
            event.target.nodeName;
        
    }
}

export default Buttons;